package com.justtennis.ui.common;

public class CommonEnum {
    public enum MODE {
        INVITE_SIMPLE,
        INVITE_DETAIL,
        INVITE_CONFIRM
    };

    public enum LIST_PLAYER_MODE {
        EDIT,
        FOR_RESULT,
        INVITE
    };
}
